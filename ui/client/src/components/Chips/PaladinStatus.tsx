import { PaladinIcon } from "../Icons/PaladinIcon";

export const PaladinStatus = () => {
  return (
    <div className="flex items-center gap-2">
      <PaladinIcon width={20} height={20} />
    </div>
  );
};
